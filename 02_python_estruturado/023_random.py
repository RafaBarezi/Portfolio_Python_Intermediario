# random() float no intervalo (00,0, 1,0)
# uniform(a, b) float N tal que a ≤ N ≤ b
# gauss(mu, sigma)	Distribuição gaussiana. mu é a média e sigma é o desvio padrão.
# normalvariate(mu, sigma)	Distribuição gaussiana. mu é a média e sigma é o desvio padrão.

# Funções para inteiros:
# randrange(stop): retorna um elemento selecionado aleatório de range(start, stop, step), mas sem construir um objeto range.
# randrange(start, stop, [step])
# randint(a, b)	retorna número inteiro N tal que a ≤ N ≤ b

# Funções para sequências
# choice(seq): retorna elemento aleatório de uma sequência não vazia seq.
# shuffle(x[, random]): embaralha a sequência x no lugar.
# sample(pop, k): retorna uma sequência de tamanho k de elementos escolhidos da população pop, sem repetição. Usada para amostragem sem substituição.